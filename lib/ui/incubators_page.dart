import 'package:flutter/material.dart';

class IncubatorsPage extends StatelessWidget{
  const IncubatorsPage({ super.key });

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(child: Text('Inkubator')),
    );
  }
}